import React from "react";
import { Button } from "ekorn";

export default function DataIngestion() {
  return (
    <div>
      This is the data ingestion app this app has a Button from our components
      library
      <Button>Click me!</Button>
    </div>
  );
}
